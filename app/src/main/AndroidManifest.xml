<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.your_precioustime">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Your_PreciousTime"
        tools:targetApi="n">
        <activity
            android:name=".SecondActivity.Busfragment.Bus_Activity"
            android:exported="false" />
        <activity
            android:name=".SecondActivity.FavoriteFragment.FavoriteDeepInfo"
            android:exported="false" />
        <activity
            android:name=".SecondActivity.FavoriteFragment.DeepStationInfoActivity"
            android:exported="false" />
        <activity
            android:name=".SecondActivity.SubwayFragment.SubwayFragment"
            android:exported="false" />
        <activity
            android:name=".SecondActivity.SecondActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>